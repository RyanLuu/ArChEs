using Arches;

using semantics Arches.Semantics;
using learners Arches.WitnessFunctions;

language Arches;

@complete feature double Score = Arches.RankingScore;

@input Image input_image;

@start Image program := single;

Image single := FilterColor(single, color)
              | Recolor(single, color)
              | Orthogonal(single, orth_option)
	      | input_image;

int color;
int orth_option;
